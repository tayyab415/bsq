# Visualizer scripts

- `serve.sh` — wrapper to start the review server
- `serve_visualizer.py` — copied from `aws/scripts/serve_visualizer.py` during sync

After sync, patch `serve_visualizer.py` if needed so `METRICS_ROOT` points at `metrics-calculation/outputs/`.
