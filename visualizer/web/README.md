# Web client

Sync from workspace:

```bash
cp -R /path/to/aws/web/visualizer/* ./visualizer/web/
```

Files: `shooting.html`, `shooting.js`, `shooting.css`, `vendor/`.

Do not commit hackathon tracking data; the client fetches frames via the local API.
