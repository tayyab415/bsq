# Fixtures

**Synthetic only** — hand-made rows for CI / `--no-s3` smoke tests.

Do not copy real hackathon XML, metadata, parquet, or score tables here. Official data stays outside the repo; real outputs go to `outputs/` (gitignored).
